# Data Directory

Place local/generated data files here if you run the notebooks locally.

This folder is git-ignored (except this file) because datasets and Spark outputs can be large.

The notebooks primarily pull source data from Hugging Face (`McAuley-Lab/Amazon-Reviews-2023`).
